# Leave OPENAI_API_KEY empty to use built-in mock AI mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional Claude (Anthropic) — currently not used; OpenAI route is wired by default.
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-6
